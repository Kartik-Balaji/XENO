{
  "private": true,
  "scripts": {
    "build": "cd frontend && npm run build && cp -r .next ../.next",
    "postinstall": "cd frontend && npm install"
  },
  "dependencies": {
    "next": "16.2.9"
  }
}
